# Kustomization pour la base Orabank SMS Banking
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml

commonLabels:
  app.kubernetes.io/name: orabank-sms-banking
  app.kubernetes.io/component: backend
  app.kubernetes.io/part-of: orabank-digital
